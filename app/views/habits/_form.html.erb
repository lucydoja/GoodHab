<%= form_with(model: habit) do |form| %>
  <% if habit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(habit.errors.count, "error") %> prohibited this habit from being saved:</h2>

      <ul>
        <% habit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="justify-content-center mt-2 mb-2" id="habit_form">
    <div class="field form-group">
      <%= form.label :date, class:"font-weight-bold" %></br>
      <%= form.date_field :date , order: [:day, :month, :year], end_year: Date.today.year, value: Date.today, required: true%>
    </div>

    <div class="field form-group">
      <%= form.label :category, class:"font-weight-bold" %>
      <%= form.text_field :category, class:"form-control", required: true %>
    </div>

    <div class="form-group">
      <p , class="font-weight-bold">Duration</p>
      <div class="row">
        <div class="field col">
          <%= form.label :hours %>
          <%= form.number_field :hours, class:"form-control flex-grow-1", value: 0, max: 23, min:0, required: true %>
        </div>
        <div class="field col">
          <%= form.label :minutes %>
          <%= form.number_field :minutes, class:"form-control flex-grow-1",  value: 0, max: 59, min:0,  required: true %>
        </div> 
      </div>
    </div>

    <div class="field form-group">
      <%= form.label :description, class:"font-weight-bold" %>
      <%= form.text_field :description, class:"form-control", maxlength:30, required: true %>
    </div>

    <div class="actions d-flex my-2">
      <%= form.submit class:'btn btn-info btn-block'%>
    </div>
    <div class="actions d-flex my-2">
      <%= link_to 'Cancel', habits_path, class:'btn btn-outline-info btn-block'%>
    </div>
  </div>
<% end %>
